# QMemo

Quick memo/note utilities with web UI and CLI tooling.

## Routes

Uses WebHandler. `@web` methods are exposed under `/qmemo/{method}`.

## CLI

- Show commands:
  php index.php /qmemo/cli -help
